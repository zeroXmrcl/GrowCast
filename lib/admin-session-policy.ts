/** Admin session lifetime in seconds (≤ 24 hours). */
export const SESSION_TTL_SECONDS = 60 * 60 * 24;

/** Policy ceiling used by tests and docs. */
export const MAX_SESSION_TTL_SECONDS = 60 * 60 * 24;
