GrowCast (Next.js 16.2.12 App Router, React 19.2.4, JSON-Datei-Persistenz, Custom-scrypt-Admin-Auth) ist fachlich klar geschnitten: öffentliche Journal-Flächen bleiben bewusst öffentlich, Mesh ist fail-closed ohne GROWCAST_MESH_TOKEN, Admin-Mutationen hängen an requireAdmin bzw. isAdminAuthenticated plus SameSite=lax. Der Skeptiker hat 0 Critical bestätigt — es gibt keinen nachgewiesenen unauthentifizierten Auth-Bypass und keine Remote-RCE.

Der dokumentierte Produktionspfad (Docker Compose, NODE_ENV=production, HOSTNAME=0.0.0.0, HTTP auf :3000) ist jedoch in mehreren High-Findings betrieblich gebrochen: Secure-Cookies verhindern LAN-HTTP-Login, die CSRF-Origin-Prüfung vergleicht gegen die Listen-Adresse 0.0.0.0 und macht Medien-Uploads unbrauchbar, ein globales 40-MB-Proxy-Klonen ermöglicht unauthentifizierte Speicherlast, öffentliche Media-GETs folgen Symlinks, und die Grow-JSON-Pipeline (nicht-atomare Writes, stilles DEFAULT_GROW, Demo-Feld-Fallbacks, Archive-Rollback) kann Echtdaten durch die Tomaten-Demo ersetzen oder ein soeben publiziertes Archiv löschen. Zusätzlich leert jedes Speichern das deaktivierte Erntedatum; parallele Admin-Tabs können einen archivierten Grow wiederbeleben oder „New Grow“ als zweites öffentliches Archiv anlegen. Health Score 52/100: für localhost und HTTPS-Tunnel nutzbar, für den beworbenen LAN-/Compose-Betrieb und als langlebiges Journal ohne Remediation nicht vertrauenswürdig.

Nachaudit 2026-08-22 (Cloudflare-Tunnel-Betrieb): unique Highs H-01–H-11 und H-13 (CAS gegen Double-Complete/Stale-Save) im Code adressiert; H-12 (Zwei-Tab-LWW auf derselben Live-ID) bewusst offen. Retest npm test 107/107, next build 0. Details: MASTER-REPORT-WORKFLOW-DE.md Abschnitt 9.